# Brief Description
- Upon opening the file on wireshark we notice some tcp streams.
- The flag isn't directly visible on the bottom right panel and we need to manually follow the tcp stream to obtain the flag and delete the extra spaces.
- What is seen?
- Hex view of a single packet, Mostly binary TCP data, Only a few printable characters on the right (# ( n & *0 h d)
